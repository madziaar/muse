/**
 * Debounce delay in milliseconds for user input to trigger actions like tag suggestions.
 */
export const DEBOUNCE_DELAY_MS = 500;

/**
 * The maximum number of generations to keep in the Muse history.
 */
export const MAX_HISTORY_SIZE = 5;

/**
 * Maximum allowed file size for video uploads in bytes (50 MB).
 */
export const MAX_VIDEO_FILE_SIZE_BYTES = 50 * 1024 * 1024;

/**
 * Default duration for toast notifications to be visible in milliseconds.
 */
export const TOAST_DURATION_MS = 5000;
